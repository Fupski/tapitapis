#include <iostream>

using namespace std;
int main() {
    int n;
    cin >> n;
    int number = 1 << n; // создаем число с единицей в n-й позиции побитовый сдвмг в лево
    cout << number << endl; // выводим результат Этот код считывает число n с помощью функции cin, 
                               //затем создает число, у которого только n-й бит равен 1, используя оператор сдвига влево. 
    return 0;                  //Наконец, он выводит результат с помощью функции cout.
}
#include <iostream>
using namespace std;
/*
int main() {
    int A, i;

    cout << "Enter A: ";
    cin >> A;
    cout << "Enter i: ";
    cin >> i;

    // Установка i-го бита в 0
    A &= ~(1 << i);

    cout << "Result: " << A << endl;  //В этом коде мы запрашиваем у пользователя значения A и i. Затем мы используем оператор
                                      //побитового И (&) и побитового отрицания (~) для установки i-го бита числа A в 0. Затем мы выводим результат на экран.

    return 0;
}*/































using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography.X509Certificates;
using System.Text;
using System.Threading.Tasks;

namespace C_shlab4
{
    class сard 
    {
        private string name;
        private int age;
        private string diagnosis;

    }
    public class Hospital
    {
        public class workers
        {
            public class Nurs { }
            public class Doc { }

            public class Chief_medical { }
            



        }
        class сart
        {
            private string name;
            private int age;
            private string diagnosis;
            private string status; 

        }
    }
    internal class Program
    {  
        static void Main(string[] args)
        {
        }
    }
}



/////////////////////////////
#include <iostream>
#include <locale.h>

using namespace std;

int main() {
    //setlocale(LC_ALL, "Russian");
    int i, j, m, n;
    int a, b;
    cout << "Count stroki : "; cin >> m;
    cout << "Count stlb : "; cin >> n;
    cout << "Min a:"; cin >> a;
    cout << "Max b:"; cin >> b;

    if (7 <= m && m <= 17) {
        if (7 <= n && n <= 15) {
            int** arr;
            arr = new int* [n];
            for (i = 0; i < n; i++) {
                arr[i] = new int[m];
                for (j = 0; j < m; j++)
                   
                    cout << cout.width(3) + (arr[i][j] = rand() % b + a);
                    cout << "\n";
                    if (n <= (a + (b - a) / 8))
                        if (n >= (b - (b - a) / 8))
                        {
                            cout << n;
                        }
            }
           /* for (i = 0; i < n; i++){
                if ( n <= (a + (b - a) / 8))
                    if (n >= (b - (b - a) / 8))
                    {
                        cout << n; 
                    }
                    
            }*/
                    
        }
    }
        else { cout << " error: Invalid array size entered"; }
    return 0; 
    
}
